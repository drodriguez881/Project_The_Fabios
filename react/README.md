# Initial downloads

At /react directory:
  npm install

# Running the server

At /react directory:
  npm dev run
  
# Adding images

At /react/img directory:
  add a jpg that matches the building name
